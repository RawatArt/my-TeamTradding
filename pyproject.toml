[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-team"
version = "0.7.0"
description = "A safety-first, AI-assisted trading research platform."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
authors = [{ name = "AI Trading Team" }]
dependencies = [
    "MetaTrader5==5.0.6180; sys_platform == 'win32'",
    "pydantic>=2.10,<3",
    "pydantic-settings>=2.7,<3",
]

[project.optional-dependencies]
providers = [
    "anthropic==1.5.0",
    "google-genai==1.75.0",
    "openai==2.54.0",
]
dev = [
    "mypy>=1.13,<2",
    "pytest>=8.3,<9",
    "ruff>=0.8,<1",
]

[project.scripts]
ai-trading-team = "ai_trading_team.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ai_trading_team.prompts" = ["artifacts/*.md", "artifacts/*.toml"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
cache_dir = ".cache/pytest"
minversion = "8.0"
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
    "mt5_integration: requires an explicitly enabled Windows MT5 demo terminal",
    "llm_smoke: requires an explicitly enabled provider credential and performs a minimal model call",
    "shadow_provider: requires an accepted provider/model and performs an explicit SHADOW cycle",
]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests"]
warn_unreachable = true

[[tool.mypy.overrides]]
module = ["anthropic", "google", "google.*", "openai"]
ignore_missing_imports = true
